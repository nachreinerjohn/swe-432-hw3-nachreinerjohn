import { render, screen } from '@testing-library/react';
import App from './App';

describe("GET / ", () => {
  test("I mistakenly required tests when I didn't plan on doing them. should just return true", () => {
    expect(true).toBe(true);
  });
});
