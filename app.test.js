const request = require("supertest");
// we also need our app for the correct routes!
const app = require("./app");

test('returns true so I can work on my project', () => {
  expect(true).toBe(true);
});
